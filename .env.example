# Opcional: hacia dónde redirige el proxy en desarrollo (por defecto http://127.0.0.1).
# Si la API usa otro puerto: VITE_API_PROXY_TARGET=http://127.0.0.1:8080
# VITE_API_PROXY_TARGET=http://127.0.0.1

# Opcional: si otros componentes usan import.meta.env.VITE_APP_BASEURL, en desarrollo usa ruta relativa para evitar CORS
# VITE_APP_BASEURL=/TurnosAPIDGP/
