
export const TabAllMesas = () => {




  return (
    <>


      {/* {mesas?.map((categoryZones, index) =>
        categoryZones.Mesas.length > 0 ? (
          <div key={index}>
            <h4 style={{ fontWeight: "600" }} className="mt-3 mx-3">
              {categoryZones.Descripcion}
            </h4>
            <hr />
            <div className="mesas mb-3 mx-3">
              {categoryZones.Mesas.map((individualMesas, index) => (
                <MesaIndividual
                  individualMesas={individualMesas}
                  index={index}
                  handleShow={handleShow}
                  key={index}
                />
              ))}
            </div>
          </div>
        ) : null
      )} */}
    </>
  );
};